services:
  - type: web
    name: clearglass-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
    envVars: []
    pythonVersion: 3.11.9